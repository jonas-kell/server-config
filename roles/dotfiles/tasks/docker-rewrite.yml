---
- name: Ensure Docker repository is amd64 only
  ansible.builtin.lineinfile:
      path: /etc/apt/sources.list.d/docker.sources
      line: "Architectures: amd64"
      insertafter: EOF
      state: present
