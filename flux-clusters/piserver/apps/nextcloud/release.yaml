---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nextcloud
  namespace: nextcloud
spec:
  interval: 5m0s
  chartRef:
    kind: HelmChart
    name: nextcloud
  valuesFrom:
    - kind: ConfigMap
      name: nextcloud-values
